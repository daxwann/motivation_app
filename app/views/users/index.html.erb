<div>
  <h1>Users</h1>
  <%= render 'shared/errors' %>
  <%= render 'shared/notices' %>

  <% unless @users.empty? %>
    <ul>
      <% @users.each do |user| %>
        <li><a href="<%= user_url(user) %>"><%= user.username %></li></a>
      <% end %>
    </ul>
  <% end %>
</div>
